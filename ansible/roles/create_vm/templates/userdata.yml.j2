#cloud-config
manage_etc_hosts: true
users:
  - name: ansible
    sudo: "ALL=(ALL) NOPASSWD:ALL"
    shell: /bin/bash
    groups: sudo
    lock_passwd: true
    ssh_authorized_keys:
      - '{{ ansible_public_ssh_key }}'
